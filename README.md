# Getting started

`git --version`

`git config`

`git init`

`git status`

`git add`

`git commit`

# VSCode

use the UI

# Working with remote repos

`git clone`

`git fetch`

`git pull`

`git push`

# Branches

`git switch`

`git checkout -b`

# GitHub first steps

registration

new repo

pull request

# Other stuff

`.gitignore`
`.README`

# Advanced
`git merge`

ssh login

protect master branch

GitHub organizations & teams

GitHub issues

GitHub pages