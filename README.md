# VSCode
setup

# Getting started

`git --version`

`git config`

`git init`

`git status`

`git add`

`git commit`

# Working with remote repos

`git clone`

`git fetch`

`git pull`

`git push`

# Branches

`git switch`

`git checkout -b`

# GitHub first steps

registration

new repo

pull request

# Other stuff

`.gitignore`

# Advenced
`git merge`

ssh login